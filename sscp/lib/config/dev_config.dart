class DevConfig {
  // Set to true to bypass login screens during development
  static const bool bypassLogin = false;
  
  // Default role when bypassing login (student, teacher, admin)
  static const String defaultRole = 'admin';
}
